namespace fb;

enum PlayerCommandType:byte { 
    // 向上移动
    MoveUp = 0,
    // 向下移动
    MoveDown = 1,
    // 向左移动
    MoveLeft = 2,
    // 向右移动
    MoveRight = 3,
}

table PlayerInput {
    frame:int;
    command_type:PlayerCommandType;
}
